# AdventureWorks Docker Environment
# Copy to .env.docker and fill in values

# SQL Server connection string (point to your existing database)
CONNECTION_STRING=Server=host.docker.internal;Database=AdventureWorks;User Id=sa;Password=YOUR_PASSWORD;TrustServerCertificate=true
