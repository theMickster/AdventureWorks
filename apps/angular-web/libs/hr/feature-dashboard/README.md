Angular feature library for the HR Dashboard. Provides `HrDashboardComponent` — headcount/status stat cards, a department headcount bar chart, a tenure distribution chart, and a pay-band summary, with manual refresh. Route: `/hr/dashboard`.

## Running unit tests

Run `nx test hr-feature-dashboard` to execute the unit tests.
