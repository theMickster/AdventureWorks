Angular feature library for the Sales Dashboard. Provides `DashboardComponent` — three DaisyUI KPI stat cards (Revenue, Orders, Avg Order Value) with skeleton loading. Route: `/dashboard`.

## Running unit tests

Run `nx test feature-dashboard` to execute the unit tests.
